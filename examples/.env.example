# Example .env file for pg-ddl-extractor
# Copy this to .env and fill in your database credentials

# ─── DEV Database ────────────────────────────────────
DEV_DB_HOST=localhost
DEV_DB_PORT=5432
DEV_DB_NAME=my_dev_database
DEV_DB_USER=postgres
DEV_DB_PASSWORD=your_password

# ─── PROD Database ───────────────────────────────────
PROD_DB_HOST=prod-server.example.com
PROD_DB_PORT=5432
PROD_DB_NAME=my_prod_database
PROD_DB_USER=readonly_user
PROD_DB_PASSWORD=your_password

# ─── Optional: Custom Output Directory ──────────────
# SQL_OUTPUT_DIR=/custom/path/to/sql

# ─── Optional: SSH Tunnel for PROD ──────────────────
# Uncomment if your PROD database requires SSH tunnel
# PROD_SSH_HOST=your-server.com
# PROD_SSH_PORT=22
# PROD_SSH_USER=your_ssh_user
# PROD_SSH_KEY_PATH=~/.ssh/id_rsa

# ─── Quick Usage ─────────────────────────────────────
# pg-ddl-extract --env dev          # Extract schema → sql/dev/
# pg-ddl-extract --env prod         # Extract schema → sql/prod/
# pg-ddl-diff                       # Compare DEV vs PROD
# pg-ddl-migrate                    # Generate migration SQL
# pg-ddl-docs --env dev             # Generate docs → sql/docs/
# pg-ddl-lint --env dev             # Lint schema
# pg-ddl-validate --env dev         # Validate conventions
# pg-ddl-deps --env dev             # FK dependency graph
# pg-ddl-size --env dev             # Storage analysis
# pg-ddl-stats --env dev            # Database statistics
# pg-ddl-search "keyword"           # Search SQL files
# pg-ddl-changelog                  # Schema changelog
# pg-ddl-watch --env dev            # Watch for changes
# pg-ddl-init                       # Initialize project
